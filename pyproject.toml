[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "naviverse"
version = "0.1.0"
description = "LangGraph Multi-Agent Travel Planner backend"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]